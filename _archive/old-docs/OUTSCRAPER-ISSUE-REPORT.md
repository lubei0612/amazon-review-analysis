# Outscraper API 问题诊断报告

## 📋 问题描述

Outscraper API调用成功返回 `Status: Success`，但返回的数据始终为空数组：

```json
{
  "id": "task_id",
  "status": "Success",
  "data": [[]]  // ❌ 空数据
}
```

## 🔍 已测试方案

### 1. 不同URL格式
- ✅ 直接ASIN: `B08N5WRWNW`
- ✅ 简短URL: `https://www.amazon.com/dp/B08N5WRWNW`
- ✅ 完整URL (含产品标题)
- **结果**: 所有格式都返回空数据

### 2. API参数配置
- ✅ 添加了 `domain: 'amazon.com'`
- ✅ 添加了 `filterByReviewer: 'all_reviews'`
- ✅ 添加了 `filterByStar: 'all_stars'`
- **结果**: 仍然返回空数据

### 3. 同步/异步模式
- ✅ 异步模式 (`async: true`) + 轮询
- ✅ 同步模式 (`async: false`)
- **结果**: 两种模式都返回空数据

### 4. 测试产品
- ✅ AirPods Pro (B08N5WRWNW) - 有超过10000条评论
- ✅ Echo Dot (B0BSHF7WHW) - 有超过5000条评论
- **结果**: 所有热门产品都返回空数据

## 📊 账户状态

根据 `GET /profile` 接口返回：

```json
{
  "account_status": "valid",
  "balance": 9.74,  // 余额充足
  "upcoming_invoice": {
    "products_lines": [
      {
        "product_name": "Amazon Reviews",
        "quantity": 628,  // 已使用628条评论
        "lines": [
          {
            "quantity": 500,  // 前500条免费
            "amount": 0
          },
          {
            "quantity": 128,  // 超出部分收费
            "amount": 0.26,
            "unit_price": "$0.002"
          }
        ]
      }
    ]
  }
}
```

### 关键信息
- ✅ 账户状态: `valid`
- ✅ 余额: `$9.74`
- ⚠️ **已使用628条评论（超出500条免费额度）**
- ⚠️ **当前按量计费模式: $0.002/条**

## 🤔 可能的原因

### 1. 免费额度用尽后的配置问题
虽然账户有余额$9.74，但可能需要：
- 升级到付费订阅计划
- 在控制面板启用自动扣费
- 预付费或充值credits

### 2. API限流或配额限制
- 可能存在每日/每小时请求限制
- 可能需要等待billing cycle重置

### 3. API已知问题
- Outscraper API可能存在bug
- 某些产品可能被屏蔽或限制

## 💡 解决方案

### 方案1: 联系Outscraper客服（推荐）
1. 访问 https://outscraper.com/
2. 联系客服说明问题：
   - API返回Success但data为空
   - 已测试多种产品和参数组合
   - 账户有余额但无法获取数据
3. 询问是否需要：
   - 升级订阅计划
   - 启用额外配置
   - 充值credits

### 方案2: 使用RapidAPI备用爬虫（立即可用）
RapidAPI提供Amazon Reviews API，已在项目中集成：

#### 步骤:
1. 注册 https://rapidapi.com/
2. 搜索 "Amazon Reviews" API
3. 获取API Key
4. 在 `.env` 中配置：
   ```
   RAPIDAPI_KEY=your_rapidapi_key_here
   ```
5. 系统会自动fallback到RapidAPI

#### RapidAPI优势:
- ✅ 有免费额度
- ✅ 按需计费
- ✅ 稳定可靠
- ✅ 已在`CrawlerFacade`中集成

### 方案3: 临时跳过Outscraper
在 `.env` 中注释掉Outscraper Key，强制使用RapidAPI：
```bash
# OUTSCRAPER_API_KEY=xxx  # 暂时禁用
RAPIDAPI_KEY=your_rapidapi_key_here  # 启用备用方案
```

## 🔧 代码修复记录

### 已修复的问题
1. ✅ 添加了 `domain` 参数到 `createTask` 方法
2. ✅ 添加了 `filterByReviewer` 和 `filterByStar` 参数
3. ✅ 优化了错误日志和调试信息
4. ✅ 测试了ASIN和URL两种格式

### 代码状态
所有代码修复已完成，问题出在Outscraper API服务端。

## 📝 建议

**立即行动**:
1. 配置RapidAPI作为备用方案（最快解决）
2. 联系Outscraper客服了解账户状态
3. 检查Outscraper控制面板的任务历史

**长期方案**:
- 升级Outscraper订阅或充值
- 使用RapidAPI作为主要爬虫
- 考虑其他第三方API服务

## 🔗 相关资源

- Outscraper官网: https://outscraper.com/
- Outscraper API文档: https://outscraper.com/api-docs/
- Outscraper Profile: https://outscraper.com/profile/
- RapidAPI Amazon Reviews: https://rapidapi.com/search/amazon%20reviews
- 项目已集成RapidAPI: `src/crawler/RapidAPICrawler.js`

---

**更新时间**: 2025-10-25
**状态**: 等待Outscraper客服响应 / 切换到RapidAPI


